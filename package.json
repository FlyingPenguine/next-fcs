{
  "name": "next-fcs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "deploy": "next build && next export && touch out/.nojekyll && rm -r docs && mv out docs && git add . && git commit -m \"deploy\" && git push"
  },
  "dependencies": {
    "fcs": "^0.1.2",
    "next": "10.0.7",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "webpack": "^5.24.2"
  }
}
